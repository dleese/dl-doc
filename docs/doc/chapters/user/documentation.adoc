= Documentation module
The _Documentation_ module in Logipad is a digital library for operational documents (for example manuals, bulletins, and regulations). It lets crew members find, open, download, and manage the documents assigned to them.

The Documentation view shows all documents assigned to the user. The list can be searched, filtered, and sorted (for example by date, document type, or category). For each document, users can open the content and view metadata such as revision/version information and related documents (if provided).

[IMPORTANT]
====
Regulatory and operational notice::
This guide describes how to use the application. It does not replace company-approved operational documentation (for example AFM/POH, OM-A/OM-B, MEL/CDL, QRH, and dispatch release). Users must follow the operator’s procedures for document control, revision management, and required acknowledgements. If a conflict exists between this guide and company-approved documents, the company-approved documents take precedence.
====

Supported document formats include:

* PDF
* Images (PNG)
* Video (MP4, AVI, MOV)
* Audio (MP3, WAV)
* Microsoft Office (Word, Excel, PowerPoint)
* ePub
* Text (TXT, RTF)
* HTML
* HTML ZIP files (compressed HTML packages)
* Most XML formats
* Boeing EDB with XML documents

The module also supports features such as search, categorization, annotation, sharing, offline access, and version history.

== Document Library Features
The Documentation module includes the following features to assist pilots in managing and accessing documents effectively:

*Document search:* Search the library by keyword, title, or phrase. Depending on configuration, search can include document content for items downloaded for offline use.

*Annotation:* Add highlights and notes for reference. Use annotations to mark important sections or add clarifying remarks.

*Sharing:* Share documents with other users (as allowed by operator policy and permissions).

*Offline access:* Download documents so they are available without a network connection.

*Document versioning:* Access previous versions (if retained by configuration) and see revision history.

*Document confirmation (acknowledgement):* Acknowledge documents that require confirmation. This supports operator processes for tracking that required documents were reviewed.

*Document expiration:* Documents can expire based on date or conditions defined by the operator.

*Document prioritization:* Prioritize documents by importance or urgency.

*Document restrictions:* Access control can limit visibility by role, user group, or other rules.

*Document types:* View supported file types directly in the app, depending on format and device capabilities.

== Documentation global settings
Global settings are configured by the Logipad administrator and apply to all users and documents.

[.center-caption]
.Documentation Global Settings
image::ios_Documentation_Global_Settings.png[Logipad global Documentation settings,75%,float="center",align="center"]

* *Search documentation content*
    ** _yes_ Content search is enabled for documents downloaded for offline use. An on-device search index is created when the document is downloaded.
    ** _no_ Content search is disabled. No on-device search index is created.

* *Mark Modified as "New"*
    ** Default is 7 days. Documents modified within this period are marked as *New* in the list.
    ** Options: _Never_, _1 day (24 hours)_, _2 days (48 hours)_, _3 days (72 hours)_, _14 days_, _30 days_.

* *Mark as "New" only on revision change*
    ** _yes_ Mark *New* only when the document revision changes.
    ** _no_ Mark *New* whenever the document is modified, even if the revision does not change.

* *Mark as "Read" when opened*
    ** _yes_ Mark documents as *Read* automatically when opened.
    ** _no_ Users mark documents as *Read* manually.

* *Mark Downloaded as "New"*
    ** _yes_ Mark downloaded documents as *New*. The document icon shows a green exclamation indicator. Options: _Never_, _1 day (24 hours)_, _2 days (48 hours)_, _3 days (72 hours)_, _14 days_, _30 days_.
    ** _no_ Do not mark downloaded documents as *New* automatically.

* *Remove backup documents after*
    ** _immediately_ Remove backup documents immediately after deletion.
    ** _1 week_ Remove backup documents after 7 days
    ** _1 month (30 days)_ Remove backup documents after 30 days
    ** _3 months_ Remove backup documents after 90 days
    ** _6 months_ Remove backup documents after 180 days
    ** _1 year_ Remove backup documents after 365 days
    ** _Never_ Never remove backup documents. Documents remain cached on the device until the user deletes the Logipad app.

* *Document Versions to store*
    ** _none_ Store no previous versions (current version only).
    ** _1_ Store the latest 1 version.
    ** _2_ Store the latest 2 versions.
    ** _3_ Store the latest 3 versions.
    ** _4_ Store the latest 4 versions.
    ** _5_ Store the latest 5 versions.
    ** _6_ Store the latest 6 versions.
    ** _7_ Store the latest 7 versions.
    ** _8_ Store the latest 8 versions.
    ** _9_ Store the latest 9 versions.
    ** _10_ Store the latest 10 versions.
    ** _all_ Store all versions.

* *Keep Bookmarks*
    ** _yes_ Keep bookmarks when a document is updated.
    ** _no_ Do not keep bookmarks when a document is updated.

* *Upload Confirmations immediately*
    ** _yes_ Upload acknowledgements automatically when connectivity is available. If offline, acknowledgements are queued and sent when the device is online.
    ** _no_ Upload acknowledgements only when the user synchronizes manually (for example by tapping *Synchronize* or using the pull-to-refresh gesture in the document list).

== Documentation Standard View (example)
The Documentation screen shows a folder tree on the left and a document list on the right.

[.center-caption]
.Documentation Standard View
image::ios_Documentation_View_Standard.png[Documentation standard view,75%,float="center",align="center"]

Key elements:

* *Library tree (left)* — folders and categories used to organize documents.
* *Document list (center/right)* — documents available to you in the selected folder.
* *Search (top right)* — search by title/keyword (and, if enabled, by content for offline documents).
* *Filters* — open filter options for the current view (for example document type, category, or date).
* *Open document viewer* — open a document (for example a PDF) in the built-in viewer.
* *Information icon (i)* — open document details (metadata such as dates, modified date, downloaded date, and version).
* *Version* — shows the document revision/version (if provided by the operator).
* *New indicator* — a marker on the document icon that indicates new or recently changed content (based on global settings).
* *To be confirmed* — indicates a document that requires acknowledgement. Confirm the document in accordance with your operator’s procedures.

=== Recommended use (operational)
. Synchronize before flight in accordance with your operator’s procedures.
. Open the required folder in the Library tree.
. Use *Search* or *Filters* to locate the required document.
. Open the document and verify the *version/revision* matches the operationally required issue.
. If the document requires acknowledgement, complete the confirmation step as required.

[WARNING]
====
Data currency and controlled documents::
Always verify that you are using the current, operator-approved version of each operational document. If you are unsure, re-synchronize and follow your operator’s escalation procedure.
====

=== Document filters
The *Filters* panel lets you control which documents are shown and how they are filtered.

[.center-caption]
.Filters panel (example)
image::ios_Documentation_Filter_By.png[Documentation filters panel,75%,float="center",align="center"]

Filter criteria (top section):

* *Validity Date* — Filter based on the document validity period (*Valid From* / *Valid To*).
* *Confirm by Date* — Filter based on confirmation due/required date (if used by your operator).
* *Priority* — Filter based on the document priority level set by the operator.

Filter documents by (main list):

* *Folder View* — Show documents in the currently selected folder.
* *All Documents* — Show all documents available to you across the library.
* *Newest Documents* — Show the most recently published or updated documents.
* *Recent Downloads* — Show documents recently downloaded for offline use.
* *Expired Documents* — Show documents past their *Valid To* date.
* *Future Documents* — Show documents not yet valid (*Valid From* is in the future).
* *Missing Documents* — Show documents expected for your role/assignment but not currently available on the device (for example not downloaded or not received yet).
* *Non-searchable Documents* — Show documents that are not included in content search (indexing disabled by configuration).
* *Favourites View* — Show documents you marked as favourites.

[TIP]
====
If you cannot find a required document, synchronize and follow your operator’s procedures for missing or out-of-date documents.
====

==== View mode: List and Symbol (grid)
The Documentation list can be displayed in two view modes:

* *List* — shows documents as rows with more text and metadata. Use this view when you need to review details such as title, dates, status, or version at a glance.
* *Symbol* (grid) — shows documents as tiles with larger icons and less text. Use this view to browse visually, especially when the document name is long or when you want larger touch targets.

To switch view mode, use the *List* / *Symbol* toggle at the bottom of the Filters panel.

